---
phase: 02-mru-ordering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/config.rs]
autonomous: true
---

<objective>
Add sort_order configuration option to control session list ordering.

Purpose: Enable users to choose between MRU and alphabetical sorting via config.
Output: Config struct with sort_order field, parsed from Zellij layout config.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sort_order config option</name>
  <files>src/config.rs</files>
  <action>
Add a `sort_order` field to the `Config` struct with an enum type `SortOrder`:

1. Create `SortOrder` enum with variants `Mru` (default) and `Alphabetical`
2. Derive `Debug`, `Clone`, `Copy`, `Default`, `PartialEq` on the enum
3. Add `sort_order: SortOrder` field to `Config` struct
4. Parse from config in `from_zellij_config()`:
   - Key: "sort_order"
   - Values: "mru" (default) or "alphabetical"
   - Case-insensitive matching

Follow existing pattern for config parsing (see `show_all_sessions` as reference).
  </action>
  <verify>
cargo build --target wasm32-wasip1 succeeds
cargo clippy --target wasm32-wasip1 -- -D warnings passes
  </verify>
  <done>
- SortOrder enum with Mru and Alphabetical variants
- Config struct has sort_order field
- Default is SortOrder::Mru
- Parses "sort_order" from Zellij config with "mru"/"alphabetical" values
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo build --target wasm32-wasip1` succeeds
- [ ] `cargo clippy --target wasm32-wasip1 -- -D warnings` passes
- [ ] `cargo fmt --check` passes
- [ ] Config struct compiles with new field
</verification>

<success_criteria>

- SortOrder enum exists with Mru and Alphabetical variants
- Config parses sort_order from Zellij layout config
- Default sort order is MRU
- All verification checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-mru-ordering/02-01-SUMMARY.md`
</output>
