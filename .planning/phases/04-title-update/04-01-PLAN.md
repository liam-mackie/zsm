---
phase: 04-title-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/main.rs]
autonomous: true
---

<objective>
Set the plugin pane title to "ZSM" using the Zellij plugin API

Purpose: Replace the default pane title (which shows the plugin file path) with a clean "ZSM" identifier
Output: Pane frame shows "ZSM" instead of "file:/path/to/zsm.wasm"
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pane title rename to plugin load</name>
  <files>src/main.rs</files>
  <action>
In the `load` function, after `subscribe(&[...])`, add code to set the pane title:

```rust
// Set pane title to "ZSM" instead of showing the plugin path
let plugin_ids = get_plugin_ids();
rename_plugin_pane(PaneId::Plugin(plugin_ids.plugin_id), "ZSM".to_string());
```

The `get_plugin_ids()` and `rename_plugin_pane()` functions are available from `zellij_tile::prelude::*`.
  </action>
  <verify>cargo build --target wasm32-wasip1 succeeds; grep "rename_plugin_pane" src/main.rs shows the call</verify>
  <done>Pane title set to "ZSM" on plugin load</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo build --target wasm32-wasip1` succeeds
- [ ] `rename_plugin_pane` call present in load function
</verification>

<success_criteria>
- All tasks completed
- Build passes without errors or warnings
- Pane title displays "ZSM" in the frame
</success_criteria>

<output>
After completion, create `.planning/phases/04-title-update/04-01-SUMMARY.md`
</output>
